// Use Multiple Conditional Ternary Operators....

function checkSign(num) {
  return (num > 0) ? "positive": (num < 0) ? "negative" : (num === 0) ? "zero":"not zero";
}

checkSign(10);


/*
  Multiple conditional ternary operators are a quicker
  way to do loads of if else statements ....
  
*/
