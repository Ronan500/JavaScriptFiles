// Iterate through an array with a for loop...

var ourArr = [ 9, 10, 11, 12];
var ourTotal = 0;

for (var i = 0; i < ourArr.length; i++) {
  ourTotal += ourArr[i];
}


var myArr = [ 2, 3, 4, 5, 6];

                                        /* Here the variable total should equal 20 because the for loop */
var total = 0;
for (var i = 0; i < myArr.length; i++){
  total += myArr[i];
}
